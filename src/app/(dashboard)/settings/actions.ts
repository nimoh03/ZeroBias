"use server";

// Bring-your-own-key support has been removed — all AI calls use the
// platform-managed DeepSeek/Gemini keys (DEEPSEEK_API_KEY / GEMINI_API_KEY
// env vars) only. This file is kept as a placeholder for future settings
// actions (branding, team, billing) once those tabs are wired up.